#include <iostream>
#include <string>

// TÄSSÄ STRING BUFFER KOKOOO EI TARVI MUUTTAA KU AINA PIENENEE

void tokenize(char* expr)
{
    for (const char* p = expr; *p != '\0'; ++p)
    {
        if (*p == 'b')
        {

        }
    }
}



int main()
{
    char expr[] = "Hamburger";

    tokenize(expr);

    return 0;
}